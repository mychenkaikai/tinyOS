#ifndef TINYOS_UEFI_BOOT_DEMO_H
#define TINYOS_UEFI_BOOT_DEMO_H

#include <stdbool.h>

#include "tinyos/boot_info.h"

bool tinyos_uefi_boot_demo_run(const struct tinyos_boot_info *boot_info);

#endif
